sudo: false
language: rust

matrix:
  include:
    - rust: stable
    - rust: beta
    - rust: nightly

script:
  - cargo test
  - |
      if [ "${TRAVIS_RUST_VERSION}" = nightly ]; then
          cargo test --manifest-path compiletest/Cargo.toml
      fi
